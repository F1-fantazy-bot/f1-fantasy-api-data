name: Deploy Logic Apps

on:
  push:
    branches: ['main']
    paths:
      - 'infra/runner/**'
      - 'infra/scheduler/**'
      - 'scripts/grant-runner-msi.sh'
      - '.github/workflows/deploy-logicapps.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      RESOURCE_GROUP: f1-fantazy-bot
      DEPLOYMENT_SUFFIX: ${{ github.run_id }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy Logic Apps (runner -> grant MSI -> scheduler)
        run: npm run deploy:logicapps
